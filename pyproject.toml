[project]
name = "door-sync"
version = "0.3.1"
description = "CiviCRM-to-UniFi sync service for door access control"
readme = "README.md"
authors = [
    { name = "Ryan Morash", email = "ryan@tvcog.net" }
]
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.1",  # floor tracks Debian trixie; see packaging/deb/README.md
    "httpx>=0.28.1",
    "waitress>=3.0.2",
]

[project.scripts]
door-sync = "door_sync.__main__:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pyrefly>=1.1.1",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "pytest-httpx>=0.30",
    "ruff>=0.15.20",
    "sphinx>=9.0.4",
    "sphinx-rtd-theme>=3.1.0",
]

[tool.ruff]
target-version = "py311"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes
    "I",   # import sorting
    "UP",  # pyupgrade
    "B",   # bugbear
]
ignore = [
    "E501", # line length handled by formatter/readability tradeoffs
]

[tool.ruff.lint.isort]
known-first-party = ["door_sync"]

[tool.ruff.format]
# ruff 0.16 formats Python code blocks inside Markdown by default. The files in
# docs/ are hand-authored prose with illustrative snippets (including
# column-aligned comments in architecture.md) and dated design specs that are a
# historical record -- neither should be rewritten by the formatter.
exclude = ["*.md"]
quote-style = "double"
indent-style = "space"
line-ending = "lf"
skip-magic-trailing-comma = false

[tool.pyrefly]
project-includes = [
    "src",
    "tests",
]
search-path = ["src"]
python-version = "3.11.0"
preset = "legacy"

[tool.coverage.run]
source = ["door_sync"]
branch = true

[tool.coverage.report]
show_missing = true
